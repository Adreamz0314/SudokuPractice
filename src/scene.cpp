#pragma once
#include<iostream>
#include"scene.h"

    void CScene::generate;
    {
        std::vector<std::vector<int>>matrix;
        for(int i=0;i<9;i++)
        {
            matrix.push_back(std::vector<int>(9,0));
        }
    }

    void CScene::play;
    void CScene::erase();
